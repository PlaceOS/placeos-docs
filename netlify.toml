[build]
  command = "cd awesome-axis && npm install && npm run build"
  publish = "awesome-axis/dist"

# No redirects needed for static Starlight site

# Optional: Set Node.js version
[build.environment]
  NODE_VERSION = "18"